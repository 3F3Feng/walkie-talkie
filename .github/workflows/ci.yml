name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    name: Build & Test (SPM)
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version

      - name: Build with SPM
        run: |
          echo "🔨 使用 Swift Package Manager 构建..."
          swift build --target WolkieTalkie

      - name: Run Tests with SPM
        run: |
          echo "🧪 运行 SPM 测试..."
          swift test

      - name: Summary
        if: always()
        run: |
          echo ""
          echo "✅ CI 完成"
          echo "构建: success"
          echo "测试: $(if [ $? -eq 0 ]; then echo 'success'; else echo 'failed'; fi)"
